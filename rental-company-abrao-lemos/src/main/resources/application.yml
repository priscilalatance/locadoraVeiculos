  server:
    port: 9090

    database:
        url: jdbc:mysql://${DATABASE_HOST}:3306/controleCar?useSSL=false&serverTimezone=UTC
        user: ${DB_USER}
        password: ${DB_PASS}   
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
